[project]
name = "which-warmup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.0",
    "polars>=1.42.1",
    "powerlaw>=2.0.0",
]
